info:
  name: all notes
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/notes"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
