info:
  name: delete by id
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/api/notes/2"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
