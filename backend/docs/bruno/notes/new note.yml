info:
  name: new note
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/api/notes"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
