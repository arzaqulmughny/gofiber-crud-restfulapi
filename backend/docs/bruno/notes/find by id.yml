info:
  name: find by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/notes/1"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
